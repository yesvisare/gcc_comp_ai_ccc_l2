# L3 M1.4: Compliance Documentation & Evidence
# Environment Configuration Template
# Copy to .env and configure with your values

# ============================================================================
# Logging Configuration
# ============================================================================
LOG_LEVEL=INFO

# ============================================================================
# PostgreSQL Configuration (for Audit Trail Storage)
# ============================================================================
# Required for production audit trail persistence
# Uses append-only tables with hash chaining for immutability

POSTGRES_HOST=localhost
POSTGRES_PORT=5432
POSTGRES_DB=compliance_audit
POSTGRES_USER=admin
POSTGRES_PASSWORD=your_password_here

# ============================================================================
# AWS S3 Configuration (for Evidence Storage)
# ============================================================================
# Required for compliance evidence export and long-term retention
# Use S3 Object Lock for immutable evidence storage

AWS_ACCESS_KEY_ID=your_access_key_here
AWS_SECRET_ACCESS_KEY=your_secret_key_here
AWS_REGION=us-east-1
S3_BUCKET_NAME=compliance-evidence-bucket

# ============================================================================
# Compliance Settings
# ============================================================================
# Audit log retention (default 2555 days = ~7 years for SOX compliance)
AUDIT_RETENTION_DAYS=2555

# Evidence export schedule (daily, weekly, monthly)
EVIDENCE_EXPORT_SCHEDULE=daily

# ============================================================================
# Service Configuration
# ============================================================================
# This module operates in OFFLINE mode (no external AI services required)
# All processing is local (PostgreSQL + SHA-256 hashing)

# No external AI API keys needed
# OFFLINE_ENABLED is not required (always offline)
